<!DOCTYPE html>
<html>
<head>
  <title>B$ Blog</title>
  <%= stylesheet_link_tag 'application' %>
  <%= javascript_include_tag 'application' %>
</head>
<body>
  <div id="main_content">
    <div id="header">
      <div id="home_button">
        <%= link_to "B$ Blog", root_path %>
      </div>
      <div class="buttons">
      <% if !current_user %>
        <button class="button"><%= link_to "Sign Up", signup_path %></button>
        <button class="button"><%= link_to "Log In", login_path %></button>
      <% end %>

      <% if current_user %>
        <button class="button"><%= link_to "Upload Image", "#" %></button>
        <button class="button"><%= link_to "New Post", new_post_path %></button>
        <button class="button"><%= link_to "Log Out", logout_path, method: "delete" %></button>
        <p class="current_name"><%= current_user.name %></p>
      <% end %>
      </div>
    </div>

    <% flash.each do |name, msg| %>
      <%= content_tag(:div, msg, class: "alert") %>
    <% end %>

    <%= yield %>
  </div>
</body>
</html>
